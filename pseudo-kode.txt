def illustrate(polymer)
-Oskar

def check_if_intact(polymer)
-Vemund (DONE)

def rotate_sub_polymer(polymer)
-Thorbjørn (DONE)

def calculate_energy(polymer)
-Thorbjørn (DONE: kan forbedres ved behov)

def try_rotate_N_times(polymer_size)
-Oskar

def plot_prosent_gyldige_rotasjoner()

def generate_flat_polymer()
-Vemund (DONE)

1h) Prøv å roter polymer, og hvis det ikke går roter tilbake. Istedet for kopi

Oppg. 2
def metropolis(polymer, V, N_s, temp)
-Thorbjørn (DONE)

def plot_energy_from_N_carlo_steps()
-- more plotting stuff

def get_max_distance(polymer) # se sources
--Vemund

